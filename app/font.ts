// app/fonts.ts
import { Aboreto, Poiret_One, Quicksand } from 'next/font/google';

export const aboreto = Aboreto({ subsets: ['latin'], weight: '400' });
export const poiretOne = Poiret_One({ subsets: ['latin'], weight: '400' });
export const quicksand = Quicksand({ subsets: ['latin'], weight: '400' });
